{
  "name": "lintons-beauty",
  "version": "1.0.0",
  "description": "Lintons Beauty API",
  "main": "script.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}